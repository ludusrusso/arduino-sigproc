// Identity Function

function(x) {
    return x;
}

// Double Function

function(x) {
    return 2*x;
}

// Math Operation Function
// performs the sin of the input signal
function(x) {
    return Math.sin(x);
}